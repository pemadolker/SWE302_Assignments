// import * as actions from '../actions';
// import * as types from './constants/actionTypes';

// test('LOGIN action creator', () => {
//   const payload = { user: { token: 'jwt' } };
//   expect(actions.login(payload)).toEqual({ type: types.LOGIN, payload });
// });

// test('REGISTER action creator', () => {
//   const payload = { user: { username: 'test' } };
//   expect(actions.register(payload)).toEqual({ type: types.REGISTER, payload });
// });
