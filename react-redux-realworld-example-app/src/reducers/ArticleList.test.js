// import articleListReducer from '../reducers/articleList';
// import { ARTICLE_PAGE_LOADED } from '../constants/actionTypes';

// const initialState = { articles: [] };

// test('ARTICLE_PAGE_LOADED updates articles', () => {
//   const action = { type: ARTICLE_PAGE_LOADED, payload: { articles: [{ title: 'A1' }] } };
//   const state = articleListReducer(initialState, action);
//   expect(state.articles[0].title).toBe('A1');
// });
